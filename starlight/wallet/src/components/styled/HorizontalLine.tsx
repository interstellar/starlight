import styled from 'styled-components'

import { ALTO } from 'components/styled/Colors'

export const HorizontalLine = styled.div`
  background-color: ${ALTO};
  height: 1px;
  margin-bottom: 40px;
`
