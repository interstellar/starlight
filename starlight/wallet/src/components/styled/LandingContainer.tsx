import styled from 'styled-components'

import { EBONYCLAY } from 'components/styled/Colors'

export const LandingContainer = styled.div`
  color: ${EBONYCLAY};
  display: block;
  margin: 90px auto;
  width: 600px;
`
